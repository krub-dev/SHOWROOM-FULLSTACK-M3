TODO - Deadline: 6 octubre
--------------------------

Backend:
- Migrar la persistencia de proyectos de JSON a base de datos (SQLite/PostgreSQL) usando Knex o Prisma. Investigar.
- Implementar endpoint /api/health que devuelva el estado de conexión con la base de datos.
- Crear script/comando para hacer seed de 10-20 proyectos de ejemplo en la base de datos.
- Añadir búsqueda en /api/projects?search=... para filtrar por title y tags (tecnologías).
- Implementar paginación en /api/projects?search=&page=&pageSize= y devolver meta de paginación en la respuesta.
- Validar que el campo title sea obligatorio en el backend.
- Añadir test de integración del CRUD o preparar colección Postman con scripts.

Frontend:
- Consumir el backend real (sin mocks): todos los datos deben ser persistentes en la base de datos.
- Añadir botón “Probar conexión” que use GET /api/health y muestre estado Conexión abierta/Sin conexión con la DB.
- Implementar búsqueda de proyectos filtrando por title y tags (tecnologías). Hacer los tags clicables para filtrar (opcional).
- Implementar paginación con controles de siguiente/anterior y mostrar estado “sin resultados” si corresponde.
- Gestionar estados de loading, error y éxito de forma clara y visual (skeletons opcionales).
- Mostrar mensajes claros y recuperables ante errores de la API (400/404/500).
- Mejorar accesibilidad mínima: foco visible, labels, contraste ≥ 4.5:1. Investigar a11y. 

--------------------------

Extras:
- Elige una prueba “tipo entrevista” y descríbela en el README:
  · Login + rutas protegidas (JWT).
  · Optimistic UI + reintentos.
  · Ordenación y filtros avanzados.
  · Accesibilidad rápida (Lighthouse ≥ 90).
- Añadir script de seed y archivo .env.example en el repo.
- Añadir capturas o GIF de lista/crear/buscar.
- Opcional: URLs de despliegue de frontend y backend.

Tips:
- Empieza por /api/health y conéctalo al frontend cuanto antes.
- Haz el seed pronto para trabajar con datos reales.
- Maneja errores de red en el frontend desde el principio.
- Ve actualizando el README durante el desarrollo.
- Mantén la UI simple (lista + formulario).
- Recuerda que el proyecto es tu showroom de Projects.

--------------------------
